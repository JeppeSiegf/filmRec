[build-system]
requires = ["setuptools"]
build-backend = "setuptools.backends.legacy:build"

[project]
name = "worker"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "celery",
    "python-dotenv",
]

[project.scripts]
enqueue = "worker.cli.enqueue:main"