version: '3.8'

services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    image: siegf/filmapi:latest
    env_file:
      - .env
    ports:
      - "5000:5000"
    restart: on-failure
    network_mode: host

