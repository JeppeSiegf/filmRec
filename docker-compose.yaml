version: '3.8'

services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    environment:
      DATABASE_URL:
    ports:
      - "5000:5000"
    restart: on-failure
    networks:
      - film_network

networks:
  film_network:
    driver: bridge